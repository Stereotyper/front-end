import * as React from "react";

// styles
const pageStyles = {
  //   color: "#232129",
};

const NotFoundPage = () => {
  return (
    <main style={pageStyles}>
      <title>Not Found</title>
    </main>
  );
};

export default NotFoundPage;
